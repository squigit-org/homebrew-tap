# Squigit Homebrew Tap

Homebrew formulae for Squigit on macOS arm64 (Apple Silicon).

## Install

```bash
brew tap squigit-org/tap
brew install squigit-cli squigit-ocr
```

## Verify

```bash
squigit-cli --version
squigit-ocr --version
```
