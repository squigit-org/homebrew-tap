# Squigit Homebrew Tap

Homebrew formulae for Squigit CLI tools on macOS arm64 (Apple Silicon).

## Install

```bash
brew tap squigit-org/tap
brew install squigit-ocr squigit-stt
```

## Verify

```bash
squigit-ocr --version
squigit-stt --version
```
